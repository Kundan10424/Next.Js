"use client"

// import { serverSideFunction } from "@/src/utils/server-utils";


export default function ClientRoute(){
    // const result= serverSideFunction(); // This will cause an error because server-side functions cannot be called from client components.
    return <h1>Client Route Page</h1>
}